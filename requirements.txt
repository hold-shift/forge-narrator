# Runtime dependencies for forge-narrator.
# Install into a Python 3.11 venv:  python3.11 -m venv .venv && pip install -r requirements.txt
#
# ffmpeg + ffprobe are HARD dependencies but are NOT pip packages — install via
# Homebrew:  brew install ffmpeg   (already present on the Air, v8.x).

boto3>=1.34

# Alignment backend. The MLX/Apple-Silicon fork of WhisperX. Installs cleanly on
# Python 3.11; uses the M2 GPU via MLX. Only needed for `generate`, not `estimate`.
whispermlx
