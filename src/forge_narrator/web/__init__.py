"""Local operator web console for forge-narrator (Spec C).

A thin FastAPI presentation/orchestration layer over the existing pipeline — it
duplicates no synth/cache/stitch/marks logic (Spec C §8). Localhost only.
"""
